Open up a terminal or command line in your project’s directory
// mochi chai will add node module folder
npm install mocha chai --save-dev
//the below for testing Node.js code:
npm install -g mocha


